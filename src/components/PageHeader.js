import React from 'react'
import logo from "../logo.gif";

export default () => (
    <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        {/* <h1 className="App-title">Apprenticeships</h1> */}
    </header>
    )